<resources>

    <style name="AppTheme" parent="@style/AppBaseTheme">

    </style>
    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- 状态栏和标题栏颜色-->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <!-- 标题颜色-->
    <item name="android:textColorPrimary">@android:color/white</item>
    <!-- 溢出菜单图标颜色-->
    <item name="colorControlNormal">@android:color/white</item>

    <!-- 箭头 -->
    <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <!-- 菜单项点击selector-->
        <item name="actionBarItemBackground">@drawable/abc_item_background_holo_dark</item>
        <item name="actionOverflowButtonStyle">@style/OverflowMenuTextAppeara</item>
    </style>
    <style name="OverflowMenuTextAppeara" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@mipmap/actionbar_add_icon</item>
    </style>
    <!-- 左边的箭头指示-->
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <!--溢出菜单样式
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:popupBackground">?attr/colorPrimary</item>
        <item name="android:dropDownVerticalOffset">28dip</item>
        <item name="android:dropDownHorizontalOffset">0dip</item>
    </style>
    -->
</resources>
